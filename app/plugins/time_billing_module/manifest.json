{
    "system_name": "time_billing_module",
    "name": "Shift Timesheets",
    "description": "Captures weekly contractor timesheets with auto rates, travel, and billing variance rollups.",
    "version": "0.0.22",
    "changelog": "Rota→timesheet: optional background sync (current + next ISO weeks); issue-week APIs merge rota; week_ending fix on single issue. Depends on HR.",
    "icon": "icon.png",
    "dependencies": [
        "hr_module"
    ],
    "enabled": true,
    "installed": true,
    "update_available": false,
    "settings": {
        "scheduler_week_prefill_enabled": {
            "value": "true",
            "editable": true,
            "tooltip": "If enabled, the weekly timesheet is prefilled from schedule_shifts (scheduled times + clock data when present)."
        },
        "scheduler_source_scheduled_edit_allowed": {
            "value": "false",
            "editable": true,
            "tooltip": "If false, staff cannot edit scheduled_start/end on scheduler-prefilled entries (they can still override actual times)."
        },
        "scheduler_week_background_sync_enabled": {
            "value": "true",
            "editable": true,
            "tooltip": "When enabled (and rota prefill is on), the app periodically ensures draft timesheets exist and pulls scheduled shifts for the current and next two ISO weeks, without staff opening each week."
        }
    }
}